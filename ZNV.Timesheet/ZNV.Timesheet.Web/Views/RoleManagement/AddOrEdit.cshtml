@model ZNV.Timesheet.RoleManagement.Role
@{
    Layout = null;
}


@using (Html.BeginForm("AddOrEdit", "RoleManagement", FormMethod.Post, new { onsubmit = "return SubmitForm(this)" }))
{
    @Html.HiddenFor(model => model.Id)
    <div class="form-group">
        @Html.LabelFor(model => model.RoleName, "角色名称", new { @class = "control-label" })
        @Html.EditorFor(model => model.RoleName, new { htmlAttributes = new { @class = "form-control" } })
        @Html.ValidationMessageFor(model => model.RoleName, "", new { @class = "text-danger" })
    </div>
    <div class="form-group">
        @Html.LabelFor(model => model.Users, "角色成员", new { @class = "control-label" })
        @Html.ListBoxFor(model => model.UserIds, new SelectList(Model.Users, "EmployeeCode", "EmployeeName"), new { @id = "users", @class = "form-control select2", multiple= "multiple" })
    </div>
    <div class="form-group pull-right">
        <input type="submit" value="保存" class="btn btn-primary btn-lg" />
    </div>
}

<script>
    $(document).ready(function () {
        $('#users').select2(select2.employee);
    });
</script>



